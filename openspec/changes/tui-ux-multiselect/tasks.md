## 1. 组件选择集

- [ ] 1.1 列表组件加多选集（`map[string]bool` 按稳定标识）：`space` toggle、`a` 全选/取消全选可见集、状态栏计数（含被过滤隐藏数）；验证：单元测试覆盖勾选/全选/过滤持久/清空
- [ ] 1.2 空集回落与单实体操作限制（`e`/`r`/详情在选择数 ≥2 时提示需单选）；验证：五类列表行为一致

## 2. 各 Tab 批量动词接入

- [ ] 2.1 env/text 批量 `d`（一次确认列全部目标）、text 批量 `x` 导出；验证：勾 3 条删除一次确认全删，提交后选择集清空
- [ ] 2.2 ssh hosts/keypairs 批量 `d`（引用保护语义不变）/`x`（确认页列生成路径清单）；验证：批量导出单条失败不中止并汇总
- [ ] 2.3 config 批量 `i`/`u`：选择集合并计划（跨组逐条列出），changed 逐条确认沿用；验证：All 视图跨组勾 3 条 `i` 出一份计划
- [ ] 2.4 MCP 批量 `x`/`u`/`X`/`U`：范围 = 选择集 ×（当前/全部 agent），计划/`F`/逐条确认语义不变；验证：勾 2 档案 `X` 计划覆盖 2×N 条目，`esc` 取消不写盘且选择集保留

## 3. 文档与回归

- [ ] 3.1 同步 `.agents/skills/senv-cli/SKILL.md`（多选键位与批量语义、scope 键并存说明）；验证：文档与实现一致
- [ ] 3.2 `make check` + 批量写安全核查（无免确认路径）+ AI 向导回归，结果写入 proposal 验证记录；验证：退出码 0
